1. write a "testbit" function .this function requires two 16-bit integer parameters

the first parameter is a 16 bit value to test;the second parameter is a value in the range 0..15 describing which bit to test.
the function should return true if the corresponding bit contains a one,the function should return false if that bit position contains a zero.
the function should always return false if the second parameter holds a value outside the range0..15


2. Implement debug log level messages in previous program.create a all log messages in "log " file in current directory only.

	0-warning level
	1-error level
	2-debug level


3. test on unsigned int a=0xaabbccdd,retrieve and print values separately like "aab","bccdd"?

